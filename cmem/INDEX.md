# cmem — Portable Project Memory for wazmrt

This folder is the **authoritative, portable project memory** for `wazmrt`. It lives inside the
project tree, so it travels with the project (USB drive, clones) and is **committed to git** — unlike
a machine-local `CLAUDE.md`.

**Format:** plain Markdown — one focused topic file per domain, so any single concern can be reviewed
and revised without wading through one giant file. Keep files small and single-topic.

## Policy (durable — set by the project owner 2026-07-02)

- **`cmem/` is the single home for ALL project memory.** When the owner (or anyone) says "**update the
  project memory**," that means: fold the latest decisions, found bugs, design changes, and current
  state into the matching `cmem/` topic file(s) — then add/refresh its one-line pointer in the Files
  table below. Convert relative dates to absolute; update existing entries rather than duplicating.
- **`README.md` is NOT project memory.** It is the public, user-facing document — a concise guide to
  *using* wazmrt (what it is, install, CLI/C-ABI usage, examples). Keep internal decision logs and bug
  post-mortems out of it; those live here.
- **`third_party/LICENSES.md` is the compliance source of truth**, not memory. `cmem/licensing.md`
  records the *why* and the strategy; the ledger of actually-reused code lives in
  `third_party/LICENSES.md`. Keep them consistent.

### The "update the project memory" trigger (binding on every agent)

When the owner says **"update the project memory"** (or a clear synonym — "update memory", "record
this", "remember this for the project"), the required action is BOTH of:

1. **Revise all relevant `cmem/` files** — fold in the latest decisions/bugs/design changes/state;
   refresh the one-line pointer in the Files table; convert relative dates to absolute; update existing
   entries instead of duplicating.
2. **Sync `README.md` where, and only where, the change is user-relevant** — install/usage, CLI/C-ABI
   surface, examples, status — so the README matches the new reality without absorbing internal detail.

### The "evaluate a reference project" trigger (binding on every agent)

Before incorporating or adapting code from any of the reference runtimes (see `reference-projects.md`),
complete the **Adoption Checklist** in `third_party/LICENSES.md` (benefit-vs-drawback + license
compliance), add a Component Ledger entry there, and update `reference-projects.md` status. "Looking at"
a project is free; "copying/porting from" it always requires the ledger entry.

### The "look for code issues" trigger (binding on every agent)

When the owner says **"look for code issues"** (or a synonym — "code audit", "audit the code", "hunt
for bugs"), perform a **COMPREHENSIVE audit across BOTH tested AND untested code paths**. The goal is to
catch issues that **won't surface in today's tests but will bite a future change**. Look for all four
categories:

1. **Workarounds / temporary hacks** — is each still needed, or now stale? (e.g. cache-clean races,
   leniency comments, `TODO`/`FIXME`, "for now" shortcuts.) Flag stale ones for removal.
2. **Dead code** — unused methods/fields/helpers, duplicates, orphaned exports. **Verify each with
   grep** before calling it dead (a symbol may be reached via `root.zig` re-export, the C ABI, or a
   test-only path).
3. **Bugs** — silently-wrong codegen, inverted logic, type-inference gaps, scanner/LEB off-by-ones,
   union-field access on the wrong active tag, stack-order mistakes, missing bounds checks.
4. **Fall-throughs (the worst failure mode)** — unhandled input that emits a stub/placeholder (a
   comment + a bare `0` / empty slice / `.none`) **instead of erroring**. Prefer converting
   silent-wrong to a **hard abort** (`error.Unsupported*` / a `diagnostics`-style failure), and guard
   genuinely-speculative probes so they fail loud, not silent. In this codebase the canonical example is
   an assembler/decoder path that should return `error.UnknownInstr` / `error.UnsupportedOpcode` rather
   than emit wrong bytes.

**Method:** for large files, **fan out parallel read-only investigators per category** (use the Agent
tool / `Explore`), then consolidate. Report each finding as `file:line` + a one-line description +
severity. **Fix the safe ones** and **keep the full suite green — diff the OUTPUT, not just exit codes**
(`zig build test` *and* re-run the affected `.wast` conformance files via `wazmrt <file.wast>`, comparing
`N passed / N failed` against the pre-change baseline; a build that still exits 0 while silently dropping
passes is a regression). Surface anything risky or ambiguous rather than fixing blind.

## Files

| File | What it holds |
| --- | --- |
| [overview.md](overview.md) | What wazmrt is, repo layout, the key source files, mental model |
| [vision.md](vision.md) | The goal — a blazingly-fast, smallest-binary wasm runtime, itself wasm-compilable, embeddable via the `universalWasmLoader-*` loaders. **Performance target: beat wasmtk's Deno/V8 execution** (win on startup + boundary, not JIT hot-loops; native build only). **Integration goal:** wasmtk runs wasm via native wazmrt (Deno FFI, not wasm-on-V8). **Candidate direction:** wazmrt as the loaders' native backend (consistency + no-dep + licensing freedom; wasmtime optional for heavy compute) |
| [architecture.md](architecture.md) | The decode → validate → instantiate → execute pipeline; module layout; the libc-free core; C ABI + freestanding-wasm build targets |
| [security-model.md](security-model.md) | **Authenticity path BUILT (2026-07-18): pin (Phase 5) + Ed25519 signatures — verify (`src/sign.zig`), publisher CLI (`wazmrt keygen`/`sign`), and build-time anchor (`-Droot-key`).** The CLI **denies unsigned by default when armed** (root key embedded or pin DB present); a bare build runs any `.wasm`. `--no-verify` overrides on the user's machine except a root `# mode: enforce`; the C-ABI/embedder path is intentionally ungated. Records the 2026-07-16 owner design conversation + resolved decisions (**trust anchor = embedded root key; format = roll-our-own Ed25519 custom section; deny-when-armed policy; NO rotation**). **SHELVED: a companion keystore (`wasm-keys.json`)** — decided then reversed 2026-07-18 after a package-manager survey showed it redundant for the single-publisher model (store = pin DB + one `-Droot-key`); authenticity path feature-complete. Optional: HSM key custody. Sandbox side + symlink traversal were already BUILT. Holds: the **wasm-as-scripts / wazmrt-as-base-runtime vision** (host-side `argv[0]`/`binfmt_misc` dispatch — **note: that symlink is NOT the guest-visible symlink of #17, so the vision does not need `path_symlink`**); **authenticity vs authority** as orthogonal, both required; **self-validation is impossible** (signature may live in the file, the trust anchor must not) → signature in a wasm **custom section**, key embedded in the binary so "protect the keystore" collapses into "protect wazmrt" (which the OS already does via Authenticode/WDAC, IMA/fs-verity, Gatekeeper); **a verification keystore holds no secrets** — only public keys, so it needs integrity, not a vault; the honest **threat model** (root defeats it — say so); and the **orchestrator invariants the runtime cannot enforce** (authority laundering via a written `.cmd`, Windows `cmd.exe` CWD-before-PATH command hijack, shared writable preopens, symlinks as landmines for the *next* privileged reader → validate targets at *creation*). Flagged **`--ro-dir`** as the highest value-per-effort item — **now BUILT in 4.4 (2026-07-17)** as rights-narrowing read-only preopens. **DECIDED 2026-07-16 (do not re-litigate — the *why-not* is recorded):** pinning is **verified-install, root-owned, NOT TOFU** (there is *no program-private file* — OS access control is per-user, not per-program, so a TOFU DB is as writable as the file it protects; privileged install-time pinning is the apt/rpm model and defeats the unprivileged attacker with ownership alone); **no encryption of the pin DB** (category error — encryption gives confidentiality, we need integrity; an attacker who can write it can delete/corrupt/**roll it back** without ever decrypting, and a SHA-256 of a public file is not secret); **no machine-binding** (the attacker *is* the user — machine-id/serials are identifiers not secrets, DPAPI/keyrings are per-user, TPM protects a key from *extraction* not *use*; only human physical presence separates human from code-as-human, and ownership already solved it). **Unifying invariant: integrity is anchored by ownership or a signature — never by secrecy** — which is precisely why an embedded *public* key is correct and every secret-on-the-user's-machine scheme is broken. **10th-pass additions (2026-07-20):** two ways the gate did not hold as documented — **FIXED** `flagRegion` scanning *guest* argv (the common no-`--` form `wazmrt prog.wasm install --yes` let a guest argument set `opt_out`; the doc comment already asserted the invariant the code didn't implement), and **FIXED** `.wast` files bypassing `verifyGate` entirely (`runScript` executed before the gate, so any wasm wrapped in a `.wast` ran unsigned under a root `# mode: enforce`; the gate now hashes the script bytes first — *lesson: the gate was attached to the "module" path, not to every path that **executes***). Also **FIXED: `random_get` is now a CSPRNG** (was Xoshiro256++ seeded from a timestamp behind an interface WASI specifies as cryptographic → predictable guest key material/nonces; now ChaCha seeded from `io.randomSecure`, `EIO` rather than weak bytes). Plus the run-path rules the 10th pass yielded: *every cross-section consistency property the validator checks must be re-checked by the run path or not depended on*, and *an import declaration is untrusted input too — check the declared signature at bind time*. **Sandbox/CLI hardening (2026-07-20):** `path_symlink` now refuses a lexically **escaping relative** target at *creation* (not just absolute) — the orchestrator-invariant concern: a landmine for the next privileged reader (`tar`, `cp -L`) that the runtime cannot enforce later; `--verify <garbage>` **fails closed** (it can only raise strictness, so ignoring a typo dropped the user's intent — same principle as the `modeFromDb` fix); and `wazmrt keygen` writes the Ed25519 private seed **0600** (was 0644 after umask — world-readable, for the one file that must not be; Windows inherits the directory ACL, so the documented HSM/placement advice still stands there) |
| [licensing.md](licensing.md) | **License = `MIT OR Apache-2.0`** (dual, ecosystem-standard; chosen 2026-07-02). Why dual, one-way compatibility with the Apache reference projects, and the compliance workflow. Ledger lives in `third_party/LICENSES.md` |
| [reference-projects.md](reference-projects.md) | The 9 candidate runtimes, their verified licenses, and per-project evaluation status (Evaluating until adopted) |
| [design-decisions.md](design-decisions.md) | Load-bearing invariants that must not be silently reverted — libc-free core (`smp_allocator`), opaque C handles + stable ABI, arena-owned decode, **interpreter architecture = Option A** (switch over a pre-decoded IR) + the **perf ladder A → A.5 partial-eval/superinstructions → B register machine → JIT**, the **proposal-scope decision** (core + function-references + WASI preview 1 + **full GC, multi-memory, exception handling, and complete SIMD all now IN and BUILT**; only WASI preview 2/3 stays deferred until browser-standard), Zig-0.16 API notes, and Windows build gotchas. **10th-pass invariants (2026-07-20):** **every tokenizer loop must make progress** (a depth cap catches recursion, not a loop that never advances `pos` — `sexpr.parseAtom` returning an empty atom on `;` hung the CLI on 12 bytes); **a host import's declared signature is untrusted input** (`wasi.callFor`'s arity column + `guardArity` — do not "simplify" it away); **the run path must re-check what only the validator checked** (decoder enforces no section-order/no-duplicate rules, so `CountMismatch` + `orelse funcType` belong in `initWithImports`/`main`, never inherited — plus: unequal-length multi-object `for` is illegal behavior, unchecked in ReleaseFast); and **`zig build test-safe` (ReleaseSafe) is the memory-safety gate** — a Debug-only pass does not prove the shipped build safe. **Later 2026-07-20 additions:** the validator's two resource caps bound a **PRODUCT** (`max_ctrl_depth × max_locals`, each frame dupes the local-init vector — raise one alone and the 767 MB amplification returns; the same `max_locals` is enforced on the run path, where `usize` would have *wrapped* on wasm32); **prefer a derived bound over a magic number** (`array_new_fixed` is bounded by the body's instruction count, which provably cannot reject a valid module); **`ValType.nullable()` returns a TYPE, not a bool — do not "fix" the element/table check** (a 10th-pass finding claimed otherwise and was verified false); **a gate that cannot pass is not a gate** (conformance now gates on the delta vs a baseline); and **a fuzz target must not swallow the failure class it exists to find** (the sweep's `Budget` allocator is the oracle for the `OutOfMemory` it catches) |
| [testing.md](testing.md) | Unit tests (**200 distinct** — 189 core + 11 C-ABI under `std.testing.allocator`, incl. a randomized lifecycle **fuzz** (#22), a real-symlink sandbox test (#17, skips on unprivileged Windows), the read-only-rights invariant (4.4), the pin verify `decide()` matrix (Phase 5), 17 exception-handling tests (Phase 6/6.1/6.2/6.3), the **complete-SIMD** round-trips (Phase 8), and the **Ed25519 signature** verdicts + `rootKeyFromHex` (`sign.zig`), and the **10 run-path memory-safety `hardening` tests** (2026-07-19/20, three passes: malicious modules trap, not OOB — incl. the `call` wild-`@memcpy`-write and branch-**destination** OOB-write cases); `zig build test` now prints **421 (417 pass, 4 skip) = 217 distinct** because the C-ABI target re-runs the core tests — was 389/200 before `src/fuzz.zig` added 2 blocks and the 10th-pass fixes added 3. **New steps (2026-07-20): `zig build test-safe`** (whole suite under **ReleaseSafe** — the step that turns the shipped build's silent UB into a loud panic; run it after any memory-safety change) and **`zig build conformance -Dtestsuite=<dir>`** (**baseline added 2026-07-20**: `-Dbaseline=<file>` + `-Dwrite-baseline=true` make it gate on **regressions** instead of a zero upstream has never reached; improvements are reported, not failed). **`src/fuzz.zig` REBUILT 2026-07-20 to mutation-not-generation.** It found the `sexpr` lone-`;` hang, but its coverage was near-zero: `Module.decode` succeeded **0/20 000** on random bytes and `wat.assemble` bailed at `NotAModule`, so instantiation and the assembler were never reached. Inputs are now corrupted copies of 8 valid import-free seed modules (assembled from WAT at test time, no fixture files) → **519 decoded / 387 instantiated / 142 assembled** per 4 000-iteration sweep, and **the sweep asserts its own coverage** so it fails the build rather than silently degrading again. Deep runs: 400 k iterations under Debug + 400 k under ReleaseSafe, no crashes. Now also runs under a 64 MB **`Budget` allocator**, asserting it is never exceeded and that live bytes return to 0 — so allocation amplification, previously invisible because `OutOfMemory` was caught and discarded, is now an oracle. **Real-world: the wasmtk WASI corpus (336 files) runs 331 fully / 3 correct uncaught-throw traps / 0 decode errors — see testing.md.** | + the **`zig build wasi-gate`** compiled-program conformance gate (real Zig+C guests always, Rust opt-in `-Drust-gate=true`) + the external conformance corpora (`wasm_mod`, `wasm_wast/testsuite-main`, `wasm_wasi`; the `module/` corpus is on removable media — not in this tree). **Spec-testsuite snapshot** — ⚠ **UPPER BOUNDS, NOT MEASUREMENTS: taken while `assert_invalid`/`assert_malformed` counted OUR OWN build failures as passes** (fixed 2026-07-21 — any negative-assertion module wazmrt couldn't build scored as a pass). **Re-measure before quoting.** Positive *and* negative conformance run: i32 459/0, call_indirect 169/0, func 171/0, **table_init 729/0, table_copy 1649/0**, elem 63, data 34/0, **imports 137, linking 100/37, start 11/0**, and the **function-references** files (`ref_null` 32/0, `ref_is_null` 18/0, `call_ref` 30/1, `return_call_ref` 38/9, `ref_as_non_null` 5/0, `local_init` 8/0), global 109/1 (f32/f64 2498/2). **GC P3 COMPLETE (2026-07-14):** i31 (+5 unit + `i31.wast` 11/0), **struct/array** (+2 fixtures, +7 unit, `gc_struct_array.wast` 11/0), **ref.test/ref.cast** (+3 unit, `gc_cast.wast` 11/0), **br_on_cast/br_on_cast_fail** (+1 unit, `gc_br_cast.wast` 4/0), **declared subtyping** (assembler `(sub …)` emission; +1 unit, `gc_subtype.wast` 5/0), **concrete `(ref $t)` value types** (`ValType`→`enum(u32)`; +2 unit, `gc_concrete.wast` 2/0). **2026-07-13:** #15 + host imports #1 + start (#3) + audit-ledger closeout + invoke-by-module-name + **function-references (P1/P2/P2.5)** |
| [text-toolchain.md](text-toolchain.md) | **Native WAT assembler + WAST runner** for the standard `.wast` format. `sexpr.zig` + `wat.zig` + `wast.zig` all DONE — **runs the official spec testsuite** (positive + `assert_invalid`/`assert_malformed`/`assert_exhaustion`; `assert_trap` gated on a real trap; `(register …)` cross-module imports). Assembler covers multi-value/type-index block types, `call_indirect` + multi-table, element segments (all forms + const-expr offsets), globals, reference types, reference-type table ops, **imported functions**, and **exception handling** (tag section + `throw`/`throw_ref`/`try_table`+catch, Phase 6.1). Next: imported tables/memories, bulk table ops |
| [known-issues.md](known-issues.md) | **Audit-finding ledger + the integration that surfaces each** (2026-07-09 audit + post-audit fixes). #1–#16 all **DONE** (cleared 2026-07-13). **Phase 3–4 opened #17–#22 (2026-07-15..16).** **#22 DONE** (C ABI lifecycle fuzz; +2 bugs). **#17 DONE 2026-07-16, upgraded to FULL secure traversal same day** (owner chose wasmtime parity): the handle-stack resolver `walkFull` follows in-sandbox symlinks but escapes are impossible by construction (`..` cant rise above the preopen; absolute targets re-base to the preopen root; per-component no-follow opens; budget->ELOOP). `path_symlink`/`path_readlink` done; adversarial-fuzzed. **4.3 + 4.4 + Phase 5 COMPLETE.** 4.4 (2026-07-17): `--env`, `--ro-dir`, `zig build wasi-gate` (Zig+C+Rust conformance). **Phase 5 (2026-07-17): pin verification BUILT** — `src/pin.zig` + `wazmrt pin` + `verifyGate` (hashes the in-memory bytes it runs = TOCTOU-safe; root-owned plaintext content-addressed DB; `# mode: off|warn|enforce`; `--no-verify` refused under enforce). Default `off`. **Phase 6 + 6.1 + 6.2 + 6.3 (2026-07-17): exception handling COMPLETE (both encodings)** — the exnref proposal end to end (decode+validate+execute+assemble: `exnref`/`exn` types, tag section id 13, `throw`/`throw_ref`/`try_table`, WAT/`.wast`), **tag imports** (extern kind 0x04), and **legacy `try`/`catch`/`catch_all`/`rethrow`** (6.3, decode+execute — inline handlers, no assembler/validator). **wasmtk WASI corpus: 331/336 run** (2 library modules, 3 correct uncaught-throw traps, 0 decode errors). **Phase 7 (2026-07-17): multi-memory BUILT** — `Instance.memories: []*Memory`; load/store/`memory.*` select by index (memarg bit-6 flag); decode+execute (WAT assembler deferred). **Phase 8 (2026-07-18): SIMD COMPLETE** — two-slot v128 model (owner-chosen) threaded through locals/arity/calls; the **entire `0xFD` set** (common + rare + all 20 relaxed ops) executes via `@Vector`, the WAT assembler authors every op, v128 globals (incl. imported) work, and v128-in-GC-field fails loud. **Authenticity signature-VERIFY BUILT (2026-07-18, `src/sign.zig`)** — Ed25519 over a `"signature"` custom section vs an embedded root key; **`wazmrt keygen`/`sign` CLI + `-Droot-key` BUILT**, and the **deny-unsigned-when-armed default policy BUILT** (armed = key or pin DB; `--no-verify` overrides except root `# mode: enforce`; embedder/FFI path ungated = run; **no key rotation** — rejected). **Keystore SHELVED (2026-07-18): NOT being built.** A package-manager survey (deep-research, 21 sources) showed a multi-key keystore is redundant for the chosen **single-publisher / package-managed** model — the store is the **root-owned pin DB + one embedded `-Droot-key`**. **Authenticity path is feature-complete.** `wazmrt pin <dir>` (recursive bulk pin) BUILT. Optional only: publisher HSM custody. **#19 DONE 2026-07-15 (4.1)** — traps print a named backtrace with real byte offsets (`at fn[31] <…bitcast_invalid> +0` / `by fn[30] <min.main> +59`), in the CLI *and* through the standard C API (`wasm_trap_origin`/`_trace`/`wasm_frame_*`, which the header declared but we'd never defined — a link error for embedders). Ended up **faster than baseline** (288 vs 262 Mops/s) after `noinline recordTrap`; see the error-path-inlining invariant in `design-decisions.md`. **#20 DONE 2026-07-15** (owner pulled it ahead of 4.2 — "a big hole we don't need to fall into") — the audit found **180** functions `wasm.h` declared that we never defined: a link/`dlopen` break for any embedder using the standard header, invisible because our own tests only called what we implement. **Now 0 of 319**, with `tests/c_abi_symbols.c` as a permanent link-time gate in `c-smoke` (verified it fails by un-exporting a symbol). Added the whole ref object model (`wasm_ref_t` + casts + refcounted `copy`/`same` + `host_info`), `wasm_table_get`/`set`/`grow` (long-deferred on `wasm_ref_t`), type-object constructors/copies, `wasm_foreign_t`, `wasm_tagtype_t`, and module serialize/deserialize/share; ~126 of them comptime-generated. **#21 DONE 2026-07-15** — owner flagged memory safety as a project goal ("we do not ever want to introduce exploitable holes"); the audit found **4 real bugs**, 3 shipped hours earlier in #20: a **double free** (vec copy aliased + vec delete destroyed outright), a **use-after-free needing no misuse** (a `Ref` dereferenced `*Instance` without owning it), an **uninitialized refcount** (`alloc.create` + field-by-field init skips struct defaults), and a leak. Root cause of the *blindness*: `root.zig` doesn't import `wasm_c_api.zig`, so **`zig build test` could never reach the C ABI**, and `c_smoke.c` runs on an allocator where a double free prints `OK`. Fixed by a `cabi_tests` target + `alloc = testing.allocator` under test. **#22 DONE (2026-07-16)**: the C ABI lifecycle fuzz (400 seeds in `zig build test`, coverage-guided under `--fuzz`, allocator as oracle). Building it found **2 more real bugs** — a module UAF (`interp.Instance` stored `&m.inner` with no owned handle; delete-module-then-call segfaulted) and a `wasm_trap_delete` double-free (freed unconditionally, ignoring the refcount `wasm_trap_copy` bumps — caught on seed 1). **#17 DONE 2026-07-16 (full secure traversal, 4.3)**: `walkFull` handle-stack resolver follows in-sandbox symlinks, escapes impossible by construction; `path_symlink`/`path_readlink` + adversarial fuzz; verified 5/5 on Windows real symlinks. Residual: final-component `path_open` TOCTOU tied to #18. **#18** a real Zig 0.16 std bug (`openFile(.follow_symlinks=false)` on Windows returns an async handle marked non-blocking → `.PENDING => unreachable` **crashes the host**); worked around in `wPathOpen` (stat no-follow + refuse symlink, then open with follow). **Now security-relevant: this workaround is exactly why #17 keeps a final-component TOCTOU residual — fixing #18 (or a real `openat2(RESOLVE_BENEATH)` in `Io`) lets `wPathOpen` open no-follow atomically and closes #17's residual for free.** Trigger-based: recheck on every Zig upgrade. **EH (Phase 6), multi-memory (Phase 7), and full SIMD (Phase 8, COMPLETE 2026-07-18) are BUILT**; the Ed25519 signature-verify mechanism (`src/sign.zig`, default-OFF) is BUILT. **Run-path memory-safety hardening DONE 2026-07-19** (2nd "look for code issues" pass, owner chose "harden the interpreter"): `checkStaticIndices` bounds-checks every static index immediate once at load; cold GC/EH/branch sites bounds-check dynamic values — `struct.set`/`array.set` via a bogus GC ref was an **arbitrary-write primitive**, now `GcOutOfBounds`; `throw_ref`/`branch`/`rethrow`/stack-base guards; Core#6 `has_v128` gate fix (v128 via global.get/call-result). 4 regression tests; verified in ReleaseFast. **2nd pass (3rd audit) DONE 2026-07-19:** the stack-HEIGHT wild-base class survived in the call opcodes (an unbounded `@memcpy` **write** into locals — CRITICAL), `branch` arity, block-entry `stack_base`, epilogue, `local.tee`, ref-cast peeks; plus `@intCast` funcref→u32 (native UB) and a bare `else`/`catch`/`delegate` underflowing the load-time precompute stack — all fixed via a `Frame.stackBase`/`peek` helper + `UnbalancedControl`; `opcode.zig` raw `0xDA` wrong-union rejected. +5 tests. **3rd-pass deferred items — all FIXED 2026-07-19** (owner chose shape-checked accessors): `wat.zig` malformed-`.wat` OOB/wild-union (reachable via `wazmrt file.wat`/`sign`/`pin <dir>`) closed with `wantList`/`wantAtom`/`wantStr`/`nth`/`fieldStr`/`strAt` helpers threaded through every parser-derived access (35 wrong-union derefs + all ungated `items[N]` indexes → `BadModuleField`; verified in ReleaseFast, no crash); `sexpr.zig` deep-nesting → `NestingTooDeep` (depth cap 1024); `Reader.readVecLen()` (count ≤ remaining) at every pre-alloc site in `Module.zig`/`opcode.zig` kills the OOM amplification. +2 tests. **4th pass (2026-07-20) re-audited the hardening's OWN refactor** and found the sibling holes it left: `interp.zig` `branch` guarded the source base but not the **destination** (`stack_base + arity > items.len` → OOB **write**; HIGH) + EH unwind shrinks that grow `items.len` (LOW); `wat.zig` 4 more raw-index sites the sed missed (`parseGlobal` empty-`gt` — its `parseImport` twin GOT the guard; `(memory)`/`(elem (table))`/`(if () ())`). All fixed + tested; CLI `-h`/`-v` clean. Lesson: a mechanical/single-variant fix leaves its mirror — sweep for the sibling. **5th pass (2026-07-20) audited files untouched this session:** `wast.zig` had the *sibling* of the `wat.zig` shape holes (9 sites, via `wazmrt file.wast`) → shape-checked accessors; **C-ABI import-extern USE-AFTER-FREE (HIGH)** — the Instance wrapper retained its Module but not the backed import externs, so deleting them after `wasm_instance_new` freed memory the instance still borrows → now retains/releases them (+ `wasm_table_get` externref `@intCast` UB, null-vec/leak LOWs); **`pin.zig modeFromDb` fail-open (MEDIUM)** — a malformed `# mode:` silently downgraded root enforce → now fails closed. Clean: sign, wasi sandbox, validate. Deferred LOWs logged. **6th pass (2026-07-20, memory-safety-only) FIXED integer-overflow UB:** `wasi.zig` `fd_write`/`read`/`pread`/`pwrite`/`seek` `@intCast`/`+` of 64-bit byte counts → `u32`/`u64` (overlapping-iovec `total` or a >4 GiB file/seek → ReleaseFast UB on the *shipped* target) → checked `std.math.cast`/`add`; `interp.zig` memory-size `min*page_size`/grow multiply overflow on the wasm32 build → `std.math.mul` + clamp `m.max` to 2¹⁶ pages. C-ABI `c6ff764e` re-audited **balanced/clean**; interp exec/instantiation memory ops **clean**. **7th pass (2026-07-20) — systematic `@intCast` census + deep `validate.zig`/`sign.zig` arithmetic re-audit:** fixed 2 attacker-reachable cast-out-of-range UB — `wat.zig` SIMD lane/shuffle `@intCast(u32→u8)` of a `.wat`-parsed index (`(i32x4.extract_lane 999)` → now `BadImmediate`) and `interp.zig` try_table `branch(@intCast(d + c.label))` overflow (unvalidated catch label → now `UndefinedLabel`); validate/sign verified memory-safe, all other `@intCast` guarded/widening. **8th pass (2026-07-20) — latent-class census, NO NEW ISSUES:** `std.debug.assert`-as-guard absent; use-after-realloc clean tree-wide (no `getPtr`/`addOne`; captures safe); every non-test `unreachable` dead-by-construction or caller-`isRef`-guarded; residual wrong-union `.?` are test-only; `wasi` `@ptrCast(ctx)` is wazmrt-controlled. **10th pass (2026-07-20) — 10 FIXED, the first pass in a while to find crashes from a handful of bytes**, all in classes the index-focused sweeps structurally could not see: **`sexpr.zig` lone `;` → infinite loop (CRITICAL — `(module) ; x`, 12 bytes, hung the CLI at 10.4 GB; `skipTrivia` handles `;;`/`(;` but `parseAtom` returned an empty atom without advancing `pos`)**; **`wasi.zig` host imports bound by NAME with the declared arity unchecked → segfault from a 4-line `.wat`** (fixed with a comptime `guardArity` on every `callFor` entry); **`main.zig` `funcType(fi).?` on an export index the decoder never cross-checks → segfault from 31 bytes**; **`interp.zig` unequal-length multi-object `for` over `functions`/`code`** (ReleaseFast iterates the first slice's length → OOB) and **`try_table`'s block type escaping `checkStaticIndices`**; **`wat.zig parseTable` 2 unguarded `items[i]`** (the sibling-missed-again pattern, 3rd pass running); **C-ABI `wasm_ref_copy` use-after-free + double-free** (retained a vec-owned handle the typed `copy` correctly dups), **`hostTrampoline` uninitialized heap disclosed to the guest**, **`wasm_tagtype_delete` a silent no-op**; **`main.zig flagRegion` scanned guest argv so a guest arg `--no-verify` disabled signature verification**. **Two of the three deferred items then FIXED 2026-07-20:** **linear memory = lazy pages + a 1 GiB budget** (owner's choice; `page_allocator` via **`rawAlloc`** — `Allocator.alloc` poisons with `0xAA` under runtime safety, which touched every page *and* made the guest read `0xAA` instead of zero: 4 GiB decl 2.85 s/4054 MB → 0.05 s/~0 MB, 1 GiB decl instantiates at ~0 MB and reads 0; budget re-checked in `memory.grow`; `--max-memory`), and **`Frame.pop` operand-stack underflow** (defined `0` + `underflowed` flag trapped at loop exit, not per instruction — 237 call sites made `Error!Value` impractical; **`@branchHint(.cold)` is load-bearing**: −12% without it, parity with it, and a per-instruction check costs −13% even hinted). **`random_get` also FIXED 2026-07-20** — `DefaultCsprng` (ChaCha) seeded lazily from **`io.randomSecure`** (NOT `io.random`, which falls back to "a less secure mechanism" and is `noRandom` = all zeros on an `Io` without randomness); entropy failure → **`EIO`**, never weak bytes; seeding via `Io` kept the freestanding-wasm target building and lazy seeding kept cold-start at 1.11 µs. **`.wast` verifyGate bypass FIXED 2026-07-20** — the `.wast` branch ran `runScript` (which instantiates and invokes, incl. `(module binary …)` payloads) *before* the gate, so any wasm wrapped in a `.wast` ran unpinned/unsigned even under a root `# mode: enforce`; the gate now hashes the script bytes first (right granularity — every module it runs is contained in them). *Lesson: the gate was attached to the "module" path, not to every path that **executes**.* Records which fixes shipped in which commit |
| [roadmap.md](roadmap.md) | Current status + next increments. **Done:** decode, validate, execute (int/float/memory, runs the `wasm_mod` corpus + spec testsuite incl. negative conformance), full text toolchain, **reference types + multi-table + imported globals + extended-const + reference-type table ops + validator/decoder strictness + element init expressions + imported functions/`register` (2026-07-09)**, **bulk table ops + passive elements + table-init exprs + const-expr/passive data segments (#15 closed) + host imports #1 complete + start function #3 + audit-ledger closeout + invoke-by-module-name + **function-references proposal (typed refs, call_ref, non-null + local-init; P1/P2/P2.5)** (2026-07-13)**. **Owner order: (1) full GC — P3 — COMPLETE 2026-07-14** (i31, struct/array, `ref.eq`, `ref.test`/`ref.cast`, `br_on_cast`/`br_on_cast_fail`; `comp_types` composite-type table with rec/sub/packed decode; arena GC heap with per-object RTT; tagged i31; declared subtyping via `(sub $super …)`; **concrete `(ref $t)` value types** via `ValType`→`enum(u32)` — the collapse limitation is resolved). **(2) C ABI — instantiate + call + host-func imports + global/memory/table objects DONE 2026-07-14** (`wasm_instance_new`/`exports`, `wasm_func_call`, `wasm_val_t`, `wasm_trap_*`, `wasm_func_new`→interp `HostFunc.native_env`, and `wasm_global_new`/`get`/`set` + `wasm_memory_new`/`data`/`size`/`grow` + `wasm_table_new`/`type`/`size` + all extern casts; `zig build c-smoke` runs `add`, host-callback `call $env.add`, exported-global get/set, and memory store/read/grow). **C ABI COMPLETE against the header 2026-07-15 (#20): all 319 declared functions defined** (was 180 short — a latent link break), gated at link time by `tests/c_abi_symbols.c`; added the `wasm_ref_t` model, `wasm_table_get`/`set`/`grow`, type constructors/copies, foreign, tagtype, serialize/share. **Deferred:** shared-mutable imported globals; externref table slots via `wasm_table_get`. **First host-FFI integration DONE 2026-07-14**: `zig build dll` → `wazmrt.dll` (shared, libc-free); `zig build ffi-demo` runs `examples/deno_ffi.mjs` (Deno `dlopen` + standard wasm-c-api → `answer()=42`). **(3) Deno/V8 benchmark — first measurement DONE 2026-07-14** (`zig build bench` + documented cross-process run): native wazmrt is **2.4× faster cold-start** on a trivial call, **1.5×** on `sum(1e6)` (Deno pays ~110 ms V8 init+JIT+marshalling/run); steady-state ~264 Mops/s (JIT wins hot loops). **Vision thesis confirmed.** **(4) WASI preview 1 — first slice DONE 2026-07-14** (`src/wasi.zig`: core `wasi_snapshot_preview1` — `fd_write`, args/environ, clocks, random, `proc_exit`, + a `NOTSUP` stub; WASI is native host imports, no interp changes; the CLI runs `_start` command modules and assembles `.wat`; `examples/hello_wasi.wat` → `hello from wasi`; +3 unit tests). **Phase 1 DONE 2026-07-14 — MILESTONE: a real LLVM-compiled `wasm32-wasi` program runs and prints in wazmrt** (`examples/hello_compiled.zig` → `Hello from a compiled WASI program!` + `bulk-memory memcpy works` + `saturating truncation works`). Completed the `0xFC` prefix: saturating truncation (`0x00–0x07`) + bulk memory (`memory.init`/`copy`/`fill`, `data.drop`; `Instance.data_dropped`) — the ops LLVM/Zig emit by default that were the real blocker (WASI was not). +3 unit tests (101). *(A Phase-1 "guest toolchain gap" claim — that Zig's `Io` writer never issues `fd_write(1)` — was **RETRACTED 2026-07-15**: the cause was the example's own `extern` signature clashing with std's, making wasm-ld emit a trapping `bitcast_invalid` stub. Pure `std.Io` stdout works.)* **Phase 2 DONE 2026-07-14**: `clock_res_get`; **`poll_oneoff`** (clock subscriptions sleep to the earliest deadline — relative + `ABSTIME` — which is what a guest `sleep()` compiles to; stdio fd subs report ready immediately); real **stdin** `fd_read` (fd 0, scatter into iovecs, EOF → 0, other fds EBADF) wired from the CLI via `Io.File.Reader`; `proc_raise` → trap. Verified by a compiled guest (`examples/wasi_clock_stdin.zig`) + 2 unit tests (**103**). **wazmrt now runs the whole compute + stdout + args + clock + stdin class.** **Phase 3 — WASI filesystem — DONE 2026-07-15**: `--dir <host>[:<guest>]` preopens; host-fd table (stdio/dir/file + rights + per-fd offset via the **positional** read/write calls); `path_open` (rights intersected with the dir fd's inheriting rights — a guest can't widen by reopening); real `fd_read`/`write`/`seek`/`tell`/`close`/`sync`/`pread`/`pwrite`, `fd_fdstat_get`/`set_flags`, `fd_filestat_get`/`set_size`, `fd_readdir`, `fd_renumber`, `path_filestat_get`, `path_create_directory`/`unlink_file`/`remove_directory`/`rename`. **We enforce the sandbox ourselves** — `Io.Dir`'s `resolve_beneath` is a silent no-op on Windows/Linux, so a dir handle is not a boundary; `resolve()` rejects absolute/`..`-escaping/NT-device/NUL paths, and (**#17 DONE 2026-07-16, upgraded to full secure traversal**) the handle-stack resolver `walkFull` follows in-sandbox symlinks while keeping escape impossible by construction. Verified by a compiled guest (`examples/wasi_files.zig`, 16/16) + real symlinks (`examples/wasi_symlink_traversal.zig`, 5/5). **Phase 4 (owner order 4.0 #22 → 4.1 #19 → 4.1½ #20/#21 → 4.2 #17 → 4.3 leftovers+symlinks → 4.4 all DONE):** 4.4 (2026-07-17) shipped `--env KEY=VAL`, `--ro-dir` read-only preopens (rights-narrowing, unit-tested), and `zig build wasi-gate` — a compiled-program conformance gate running real Zig+C (always) and Rust (opt-in) `wasm32-wasi` guests through the CLI with exact-stdout asserts. **Phase 4 complete.** **Since then (all DONE):** Phase 5 pin verification, Phase 6/6.1/6.2/6.3 exception handling, Phase 7 multi-memory, **Phase 8 SIMD (COMPLETE 2026-07-18 — the whole `0xFD` set + assembler + v128 globals)**, and the **Ed25519 signatures** — verify (`src/sign.zig`) + `wazmrt keygen`/`sign` + `-Droot-key` + **deny-unsigned-when-armed policy**. **Authenticity path feature-complete** (single-publisher model: pin DB + one embedded key; keystore shelved as redundant). `wazmrt pin <dir>` (recursive bulk pin) BUILT. Optional only: publisher HSM custody. **Update 2026-07-20 — audit passes 4–10:** passes 4–9 were increasingly clean censuses of the explicit-index surface, ending in two no-new-issue passes; the **10th pass then found 10 issues incl. two segfaults and a 12-byte hang** by changing lens (non-advancing parser loop, name-bound imports, cross-section properties only the validator checked). Then all three owner-decision items were taken (**lazy pages + a 1 GiB budget**, **`Frame.pop` underflow**, **`random_get` → CSPRNG**) plus the last two audit items (**`.wast` now gated**, **fuzz targets rebuilt to mutation-not-generation** — they had reached essentially nothing; now 519 decoded / 387 instantiated / 142 assembled per sweep, and the sweep asserts its own coverage) — 207 distinct tests at that checkpoint, green under Debug *and* ReleaseSafe. **Then the whole still-open list was closed 2026-07-20:** `validate.zig` resource caps (`max_ctrl_depth` 1024 + `max_locals` 50 000 — **read together, the snapshot cost is their PRODUCT**; `array_new_fixed` bounded by instruction count, which cannot reject a valid module) and the same locals cap on the **run path** (where `usize` would have *wrapped* on the wasm32 build); `wat.zig` `(table N …)` copy cap; `path_symlink` refusing lexically escaping targets at creation; `writeStringVec` u64 offsets; the `wasm_instance_new` `host_funcs` leak; `--verify <garbage>` failing closed; `keygen` writing the private seed 0600; the **conformance baseline**; and a **`Budget` allocator oracle** in the fuzz sweep. **One 10th-pass finding was WRONG and is recorded as such** — the "accept-invalid element type" report assumed `ValType.nullable()` was a bool predicate; it returns the *nullable form of the type*, so the check was always correct (verified empirically; a comment at the site says don't re-fix). **209 distinct tests** (407 printed). *Standing lessons: a run of clean passes means the lens is exhausted, not that the code is clean — and an audit finding is a hypothesis, not a fact.* **Cleanup batch 2026-07-21 (owner order 9–13 → 1–3 → 4–6; #8 deferred to upstream Zig):** `opcode.zig` raw internal-tag bytes (`0xd7..0xfa`) now **rejected** — the old guard tested immediate *kind*, a proxy that structurally could not catch tags whose kind is also reachable from a real op (`0xe3–0xe5`, `0xed`, `0xf0–0xf2`); duplicated `naturalAlign`/`naturalAlignLog2` consolidated into `opcode.naturalAlignLog2` (the `wat.zig` name was wrong — it returns a log2, not a byte count); `Module.body_offset` + `opcode`'s offset list now **saturate** instead of `@intCast` (>4 GiB module = out-of-range cast = UB, reachable via the C ABI though not the CLI); `conformance` no longer discards a whole run on one unreadable directory; the two fuzz targets split so they stop sharing a coverage corpus. **Validator-correctness batch (items 1–3, 2026-07-21)** — all three were `validate` not meaning what a `wasm_module_validate` embedder would assume: `br_on_non_null` was reject-**valid** (hard-coded `funcref`/`externref`, rejecting every GC/typed-ref label; spec wants *any* reference); SIMD memory ops and `memory.size`/`grow` had **no memory check at all**, and the scalar path only tested `memories.len == 0` rather than bounding the memarg's memory index (multi-memory) — all now via `requireMemory(index)`, with `opcode.simdIsMemoryOp` kept beside `decodeSimd` so the ranges cannot drift; and `ValType.concreteRef`'s 28-bit mask **truncated a large type index to a small VALID one** (type confusion, worse than the "wrong output" originally logged) — the text path now checks `max_concrete_index` first, the binary decoder already bounded it. **Hardening batch (items 4–6, 2026-07-21) — CLOSES the remaining list:** the last three u32-before-widening guest-array offsets (`gatherIovecs`/`fd_read`/`poll_oneoff`) now go through `Wasi.arrayOffset` (u64, whole-element fit — so `iov + 4` stays safe), finishing the class the 6th and 10th passes started; **C-ABI trap frames now RETAIN their instance** (a `wasm_trap_t` outlives its call and `wasm_frame_instance` hands the pointer back, so `wasm_instance_delete` after catching a trap left frames dangling — the last borrower in the file); and `Wasi.init` propagates OOM instead of returning a `Wasi` with **no stdio** (every `fd_write(1)` → `EBADF`, a host failure disguised as a guest bug). *Test-quality lesson: the first trap-lifetime test compared `wasm_frame_instance(...)` to the original pointer, which **passes against freed memory** — a lifetime test must dereference, not compare; verified by removing the retain and watching it crash.* **11th pass (2026-07-21) — new lenses, and the first wrong-ANSWER bug.** Every prior pass hunted crashes; none asked whether the interpreter returns the RIGHT VALUE. **`fNxM.min`/`max` used Zig's `@min`/`@max` (minNum: "NaN → return the other") instead of the NaN-propagating `fmin`/`fmax` the SCALAR path uses** — `f32x4.min(nan,1.0)` returned **1.0**, `f32x4.min(+0,−0)` returned **+0**, so identical source compiled with and without autovectorisation disagreed. **It survived eleven passes because no `simd_*.wast` has ever been run and every SIMD unit test used finite operands — exactly where `@min` and `fmin` agree.** Also fixed: a **cross-allocator realloc of guest memory** (collateral from the lazy-pages change — the C ABI still used `alloc` while `memObj()` returns interp-owned pages), an `errdefer memories[1..0]` (start > end; a one-line `.wat` panicked), and **null inverted BOTH ways in the C-ABI val path** (`ref.null` is the sentinel `maxInt(u64)`, so a guest null arrived non-NULL and a host NULL became funcref #0). Plus four headers that actively lied — incl. **`Module.supertypes` documented "unused" while `isSubtype` depends on it**, found while a dead-code audit ran on that file. **12th pass (2026-07-21) — four new lenses, most productive yet:** `wasm_module_validate` **NEVER VALIDATED** (so the 11th pass's validator batch was unreachable from the C ABI — verify the call graph, not the name; switching it on exposed an invalid fixture in our own c_smoke that had always passed); GC accessors + `call_ref` popped the FAMILY HEAD, a **reference-forgery primitive that passed validation and executed** (an i64 field read as a funcref and CALLED); a **validator PANIC on valid input** (`memory.init` wrong union — UB that silently works in ReleaseFast, found by two agents independently); **three siblings of earlier fixes** (`spectestMemory` cross-allocator, `copyFrame` un-retained, `onCallError` cross-instance `.?`); **reentrancy** wiping a suspended invocation's exceptions and resetting the depth budget (host stack overflow at ~600 re-entries); `wasm_instance_new` never running the **start function**; and ~10 further validator faults. **The whole ~40-item backlog was ALSO verified and cleared in batches A–E** — incl. `ref.test (ref nofunc)` answering 1 where the spec says 0 (and the matching `ref.cast` succeeding where it must trap), a missing imported **global** silently reading 0, an undefined heap type decoding as `externref`, an undefined `0xFD` sub decoding AND validating, **five assembler silent-drops that emitted modules not matching their source** (a vanished export, the WRONG `call_indirect` signature, a load from the wrong address, multi-memory collapsed onto index 0), five WASI syscalls answering the wrong question with ESUCCESS (incl. `clock_time_get(CPU_TIME)` returning wall time and `poll_oneoff` claiming it slept when cancelled), a C-ABI exported **tag** callable as a function, and the CLI exiting **0** on every failure **including a verify-gate refusal**. One claim did NOT reproduce and is recorded as such. **12th-pass deferred list CLOSED 2026-07-21 (425 printed / 421 pass):** SIMD memarg alignment (authority moved to `opcode.simdNaturalAlignLog2`), `ref.test`/`ref.cast` operand hierarchy, `br_on_cast` fall-through nullability (was reject-VALID), `opcode.freeBody` for `br_table`/`select_types`, `.wast` guest-memory leak (`Runner.instances`, registered *before* `runStart` so a trapping start still frees), **cross-module exceptions now CATCHABLE** (`pending_exn` is per-`Instance` but belongs to the *invocation* — `callFunction`'s `.wasm` arm hands it to the caller; payload lifetime was never the issue since the callee runs on the caller's arena), **`C.refs` enforced for `ref.func`** (§3.4.10 — we checked the funcidx existed, never that it was *declared*, so a body could forge a reference to any function in the module; enforcing it exposed two of our own fixtures as spec-invalid), and the **GC heap capped** at `max_gc_objects` (no collector remains a scope decision, but *unbounded* was the exposure; deliberately not an `isRuntimeTrap`). *Lesson repeated a fourth time: the highest-value finds keep being incomplete sweeps of my own earlier fixes.* **13th pass (2026-07-21) — the pass that finally RAN the official spec testsuite.** Twelve passes had *reviewed* this code; none had *executed* the upstream oracle, and doing so found a crash and two wrong-value classes within minutes: a **guest-controlled stack overflow** (`max_call_depth` 1024 was calibrated for ReleaseFast but sits above Debug's ~878-frame native ceiling, so the guard never fired — `call.wast` exists to test exactly this; now 512, identical across build modes), **names never validated as UTF-8** (§5.2.4, 528 accept-invalid assertions), and **hex float literals truncated rather than rounded by `std.fmt.parseFloat`** — so the *assembler* emitted a constant one ULP low and the same number in decimal vs hex compiled to different modules (third upstream Zig issue, worked around in `wat.parseFloatLit`, shared with `wast.zig`). Also `v128.const` was rejected in const-exprs (reject-valid on the form LLVM emits for any SIMD global). **The `.wast` runner had no v128 support at all** — a v128 is two slots, so every SIMD assertion failed as `arity 2 != expected 1` and none had ever run; adding it took SIMD from **848 passed / 23 985 failed to 24 951 / 2**. Four parallel agents then found: a **REJECTED module installing entries into another module's table** (per-entry bounds check → partial write survives on an *imported* table → the victim dispatches through slots it never populated), **v128 slot-vs-index conflation in both out-of-module consumers** (`wasm_func_call` returned 3 instead of 22 and punned half a vector as a pointer; the CLI's unequal-length `for` panicked in Debug and in **shipped ReleaseFast** printed raw slots and **exited 0**), **`dropSelectWidths` swallowing OOM into a silently wrong answer** (invisible to *sticky* FailingAllocator sweeps — it needs a *budgeted* allocator), **`nearest` returning a signaling NaN** (found by a V8 differential oracle over ~121 k assertions that cleared every other numeric op), **`makeTrap` reporting a host OOM to embedders as a SUCCESSFUL call**, an **uncapped `exn_store`** retaining ~124 MiB, **callee trap traces never reset**, and **`sign --key`/`keygen --out` silently ignored** (making `sign` unreachable). *Standing lesson, now demonstrated twice: an unexecuted oracle is not coverage — and the vacuous-test trap recurred (a round-trip through the same parser passes even when the parser is wrong), so check against an INDEPENDENT oracle.* **Score: 57 827 passed / 752 failed over 258 spec files; 435 local tests green under Debug AND ReleaseSafe.** **Assembler-gap batch (2026-07-21) — the reported list CLOSED, corpus 468→488/493:** inline `(export …)` on a tag no longer drops the export AND its params (15 corpus files lost an `__exn_tag` export); module-level exports resolve after all fields so a forward reference works (binaryen's shape — largest single blocker); `(export "mem" (memory $name))`, data-segment names, flat `br_table`, and `anyfunc`↔`funcref` all handled; the memory-index immediate is now *read* (`(memory.size $nope)`/`(memory.size 7)` were silently accepted against memory 0). **Legacy folded `try`/`catch` reached the assembler AND the validator** — the interp executed it since Phase 6.3, but the assembler had no text form and the validator had no arms (so a legacy-EH module ran raw yet failed `validate`/`.wast`/summarize); both closed with `try_legacy`/`catch_legacy` frames + new `MismatchedCatch`, and a compiled WASI `15_recover.wat` now prints through the normal path. `delegate` is *rejected at assembly* because `throwException` never routes it (recorded, not emitted-wrong). Each fix verified by EXECUTING the result, not just assembling. **Still open: #8 (upstream Zig), `skipConstExpr`'s GC-immediate gap, runtime `delegate` routing, and a 5-file long tail (memory64, multi-memory text, a GC type-name edge, two malformed files)** | |

## Related files outside cmem

- `README.md` — the public, user-facing doc (install, CLI/C-ABI usage, examples). NOT project memory.
- `third_party/LICENSES.md` — the compliance ledger + adoption checklist + verified SPDX inventory.
- `LICENSE-MIT` / `LICENSE-APACHE` / `NOTICE` — the dual license texts and attribution notice.
