# wazmrt conformance baseline — EXPECTED failures per .wast file.
# Generated by `zig build conformance -Dtestsuite=<dir> -Dwrite-baseline=true`.
# The step fails only when a file does WORSE than its line here, so this
# gates against regressions rather than against a zero that upstream has
# never reached. A file that improves is reported, not failed — re-generate
# to lock the improvement in. `error <path>` = the runner cannot parse it.
#
# ⚠️ A BASELINE IS ONLY HONEST IF EVERY LINE HAS A REASON. Otherwise it is a
# way to make a red number green. Every entry below falls into one of three
# groups, and NONE of them is a wazmrt defect:
#
#   1. UNTARGETED PROPOSALS (custom-descriptors, custom-page-sizes) — 81
#      assertions. wazmrt does not implement these proposals, so their modules
#      do not build. Not a defect, and NOT a pass either: banking our own gaps
#      as passes is the green-washing `wast.zig`'s `isOurLimitation` comment was
#      written after. They are refusals, recorded as such.
#
#      WHAT IS MISSING, concretely — the 81 are two proposals, not 81 things:
#
#      custom-descriptors — 79 assertions across 13 files. A descriptor is a
#        runtime value describing a struct type, so casts can be EXACT (this
#        type, not a subtype) and RTTs become first-class. Needs, in the type
#        section: `(descriptor $d)` / `(describes $s)` on a struct type, and the
#        `(exact $t)` reference-type former — which is the big one, since exact
#        refs change SUBTYPING, not just parsing, and `refMatches` / `subtypeOf`
#        / the `TypeRegistry` canonicaliser all have to learn that
#        `(ref (exact $t))` is NOT satisfied by a subtype of `$t`. Instructions:
#        `struct.new_desc`, `ref.get_desc`, `ref.cast_desc`, `br_on_cast_desc`,
#        `br_on_cast_desc_fail`, `array.new_exact`. Depends on GC, which wazmrt
#        has — this is an extension of a shipped proposal, not a new subsystem.
#
#      custom-page-sizes — 2 assertions in 1 file. A memory declares its page
#        size: `(memory 1 (pagesize 1))`, for 1-byte pages on tiny targets
#        instead of the fixed 64 KiB. Small and self-contained: a limits field
#        threaded through decode/validate/instantiate and into every bounds
#        check and `memory.grow`. Already recognised and refused by name
#        (`UnsupportedProposal`), which is why it scores as a skip elsewhere.
#
#   2. ERA-PINNED SNAPSHOT (proposals/threads) — 8 assertions. That directory
#      predates multi-memory and multi-table, so it asserts
#      `(module (memory 0) (memory 0))` is INVALID. wazmrt implements both
#      proposals, so accepting those modules is correct and refusing them would
#      be the regression. **Here the runtime is ahead of the file.** Decisive
#      check: the assertions appear nowhere in the main testsuite.
#
#   3. DELIBERATE DEVIATIONS — NONE. There is no longer any line here where
#      wazmrt is knowingly wrong by the spec. The last one was
#      `obsolete-keywords.wast`: wazmrt accepted `anyfunc`, the pre-standard
#      spelling of `funcref`, to keep two MVP-era `.wat` files in the wasmtk
#      corpus assembling. It is now refused as `ObsoleteKeyword`, whose message
#      names the modern spelling — the compatibility hint survives, the
#      deviation does not. Those two corpus files need `funcref`.
#
# `error annotations.wast` is the custom-annotations proposal: deliberately
# exotic tokens the runner cannot lex. Untargeted, same as group 1.
#
# ⚠️ Adding a line here to make the build green is the failure mode this file
# invites. A new entry needs a group above, or it is a defect being hidden.
1 proposals\custom-descriptors\array_new_exact.wast
2 proposals\custom-descriptors\binary-descriptors.wast
1 proposals\custom-descriptors\br_on_cast.wast
5 proposals\custom-descriptors\br_on_cast_desc_eq.wast
5 proposals\custom-descriptors\br_on_cast_desc_eq_fail.wast
1 proposals\custom-descriptors\br_on_cast_fail.wast
21 proposals\custom-descriptors\descriptors.wast
3 proposals\custom-descriptors\exact-casts.wast
5 proposals\custom-descriptors\exact-func-import.wast
17 proposals\custom-descriptors\exact.wast
3 proposals\custom-descriptors\ref_cast_desc_eq.wast
7 proposals\custom-descriptors\ref_get_desc.wast
8 proposals\custom-descriptors\struct_new_desc.wast
2 proposals\custom-page-sizes\custom-page-sizes.wast
6 proposals\threads\imports.wast
2 proposals\threads\memory.wast
error annotations.wast
